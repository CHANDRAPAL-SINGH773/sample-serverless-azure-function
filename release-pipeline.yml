trigger:
  - master

pr: none

steps:

- script: |
    sudo chmod npm install -g serverless
    npm install
  displayName: 'Install dependencies'

- script: |
    serverless deploy
  displayName: 'Deploy with Serverless'


